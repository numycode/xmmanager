[build-system]
requires = ["setuptools>=61,<81", "wheel", "py2app>=0.27"]
build-backend = "setuptools.build_meta"

[project]
name = "xmmanager-mb"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "py2app>=0.28.10",
    "rumps>=0.4.0",
    "wheel>=0.47.0",
]
